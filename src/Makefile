all:
	javac client/calls/*.java
	javac client/ui/*.java
	javac client/*.java

run:
	java -cp . client/Client

clean:
	rm -f .*~ *.class
	rm -f client/.*~ client/*.class
	rm -f client/ui/.*~ client/ui/*.class
	rm -f client/calls/.*~ client/calls/*.class
