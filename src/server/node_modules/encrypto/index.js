/* 
* @Author: Srikanth
* @Date:   2016-02-17 20:15:16
* @Last Modified by:   Srikanth
* @Last Modified time: 2016-02-17 20:47:14
*/

// Nodejs encryption with CTR
var crypto = require('crypto'),
    algorithm = 'aes-256-ctr',
    mypassword = 'd6F3EnJamOorifeqSrIKaNth';

exports.encryptdata = function(text,password){
	text = JSON.stringify(text);
	if (password === undefined) {
		password = mypassword;
	}
	var cipher = crypto.createCipher(algorithm,password)
	var crypted = cipher.update(text,'utf8','hex')
	crypted += cipher.final('hex');
	return crypted;		
}
 
exports.decryptdata = function(text,password){
	  if (password === undefined) {
	  	password = mypassword;
	  }
	  var decipher = crypto.createDecipher(algorithm,password)
	  var dec = decipher.update(text,'hex','utf8')
	  dec += decipher.final('utf8');
	  return dec;
}